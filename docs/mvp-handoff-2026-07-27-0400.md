# MVP continuation handoff — 2026-07-27 04:00 HKT

## Stop checkpoint

Work stopped at `2026-07-27 04:00 HKT` because the owner required all current
MVP work to stop at that time and a continuation handoff to be written. No
`API-01` implementation was started. This handoff and its companion progress
updates are the only changes made after the cutoff.

The Goal remains active because pausing a Goal is controlled by the user in the
Codex UI. Do not infer completion from the stopped session.

## Repository coordinates

| Role | Branch | Head at cutoff | Worktree |
| --- | --- | --- | --- |
| Integration | `dev` | `96952943723ef7fca3ff6617f55be7ba21088e94` | `C:\Let-you-cook_React-native` |
| Completed Foundation track | `codex/mvp-foundation` | `96952943723ef7fca3ff6617f55be7ba21088e94` | `C:\Let-you-cook_React-native\.worktrees\mvp-foundation` |
| Active API contract track | `codex/mvp-api-contract` | `803a61940b599935bbc7c9ed15d37dbf8b91e815` before this handoff commit | `C:\Let-you-cook_React-native\.worktrees\mvp-api-contract` |

`dev` and `codex/mvp-foundation` point to the same reviewed Foundation head.
The API branch is based on that head and adds only the committed `API-01` plan
and planning progress updates. No branch was pushed and no pull request was
opened. At the cutoff, `dev` was 17 commits ahead of `origin/dev`; the API
branch had no upstream.

## Completed Foundation track

The Foundation exit gate is complete and `BASE-01` through `BASE-06` are checked
in `docs/mvp-roadmap.md`.

- `BASE-01` installed the root and server lockfile dependencies, restored
  passing TypeScript checks, added the CSS side-effect declaration required by
  TypeScript 6, and restricted root Jest discovery so Windows-native test
  discovery is deterministic.
- `BASE-02` added reproducible local PostgreSQL 17 and Mailpit `v1.30.0`
  services in `compose.dev.yaml`, plus documented root service commands and
  environment examples.
- `BASE-03` created the additive `Codex_API_36` Android AVD, built and installed
  the Expo development client natively, exercised the documented route matrix,
  and fixed two protected Recipe Detail/Reviews links that had omitted the
  `/private` path segment. Evidence is in
  `docs/verification/foundation-android-smoke.md`.
- `BASE-04` centralized validated mobile API-host configuration in
  `src/config/env.ts`. The Android emulator default is
  `http://10.0.2.2:8787`, shared by the auth and general API clients.
- `BASE-05` corrected stale documentation paths to the Expo Router source under
  `src/app`.
- `BASE-06` added guarded local development reset/seed tooling and deterministic
  verified/unverified accounts. The guard limits destructive reset targets to
  the documented local `letyoucook` PostgreSQL database, and CLI output is
  sanitized.

The Foundation task history is preserved in
`docs/superpowers/plans/2026-07-26-foundation.md`. Detailed command evidence is
in the ignored local Foundation report
`.superpowers/sdd/2026-07-26-foundation/foundation-exit-report.md` inside the
Foundation worktree.

## Foundation final review and safety fix

The broad Foundation review found three Important blockers. Commit
`96952943723ef7fca3ff6617f55be7ba21088e94`
(`BASE-06: Close Foundation safety review findings`) fixed all three in one
review wave:

1. The reset guard now permits only `postgres:`/`postgresql:` URLs and rejects
   query-string `host`, `port`, `db`, and `database` addressing overrides.
   This closes the demonstrated `socket://localhost/letyoucook?db=production`
   parser bypass.
2. PostgreSQL `5432` and Mailpit `1025`/`8025` publish only on
   `127.0.0.1`.
3. Every root Compose wrapper explicitly selects project `letyoucook-dev`, so a
   hostile `COMPOSE_PROJECT_NAME` cannot redirect the destructive reset.

The fix-wave RED run passed 15 of 17 backend tests and failed on the two
intended missing protections. GREEN passed 17 of 17.

## Verified evidence

Foundation exit and final-review evidence currently establishes:

- `npm.cmd run check`: passed.
- `npm.cmd test -- --runInBand`: 16/16 suites and 67/67 tests passed.
- `npm.cmd run server:check`: passed.
- `npm.cmd run server:test`: 17/17 tests passed after the final safety fix,
  with no failures or skips. The earlier application exit checkout had passed
  14/14 before the three new guard regressions were added.
- An authorized clean-volume reproduction removed and recreated only the
  `letyoucook-dev` containers, network, and
  `letyoucook-dev_letyoucook-postgres-data` volume. Both services became
  healthy, migrations applied to the empty database, and the guarded seed
  created exactly two users and two profiles with the intended
  verified/unverified states.
- The full Android route matrix passed on application source commit
  `1aededb`. A final native subset passed on application source commit
  `34695fcd92ee2fec6587b6945de5a52c658ecee8`: Login, demo login to Home,
  Recipe Detail, Reviews and both reverse routes, Add Recipe Basics through
  Images, Profile, and logout. The later Foundation commits were documentation
  or backend/local-environment safety changes and did not change mobile
  application source.

No Expo web build or browser test was run; web is an invalid target for this
Expo 56 project because it contains native modules.

## Local environment at the cutoff

Read-only checks at `04:00 HKT` found:

- `letyoucook-postgres`: running and healthy, published only at
  `127.0.0.1:5432`.
- `letyoucook-mailpit`: running and healthy, published only at
  `127.0.0.1:1025` and `127.0.0.1:8025`.
- Compose project: `letyoucook-dev`. The running containers were created from
  the Foundation worktree's `compose.dev.yaml`; the API worktree shares these
  same local development services.
- Android device `emulator-5554`: connected, API 36, boot complete. The AVD is
  `Codex_API_36`.
- No listener was detected on backend port `8787` or Metro port `8081`.
- The prior `agent-device` verification session was closed.

Do not reset the database merely to resume `API-01`; the routing tests are
designed to avoid database access for the focused cases. If the broad backend
suite needs PostgreSQL, use the already-running documented local service.

## Active API-01 plan

The implementation plan is
`docs/superpowers/plans/2026-07-27-api-contract-versioning.md`, committed as
`803a61940b599935bbc7c9ed15d37dbf8b91e815`
(`API-01: Plan versioned application routes`).

The selected design is a composed `v1Routes` Hono router mounted once at
`/v1`. It must expose the existing `auth`, `profiles`, `recipes`, `images`,
`favourites`, `reports`, and `blocks` route families while:

- keeping `GET /health` exclusively unversioned;
- keeping current unversioned application routes as temporary compatibility
  aliases until `AUTH-01` migrates mobile callers;
- preserving request/response bodies, DTOs, auth behavior, placeholders, and
  `AppType`;
- making no mobile URL, schema, or migration change.

`API-01` is unimplemented at this checkpoint:

- `server/src/routes/v1.ts` does not exist;
- `server/src/app.ts` and `server/src/app.test.ts` are unchanged from the
  Foundation base;
- `API-01` remains unchecked in `docs/mvp-roadmap.md`;
- no RED/GREEN command for the plan has run;
- no implementation review has run.

## Exact resume action

Resume in
`C:\Let-you-cook_React-native\.worktrees\mvp-api-contract` on
`codex/mvp-api-contract`. Following the owner's all-subagent requirement,
dispatch one subagent with this bounded task:

> Implement Task 1, “API-01 — Introduce the versioned application router,”
> exactly from
> `docs/superpowers/plans/2026-07-27-api-contract-versioning.md`. Work only in
> the API contract worktree. Follow Steps 1–7 with RED-GREEN-REFACTOR, preserve
> the legacy aliases and unversioned-only health boundary, run the focused and
> full non-web gates, update the listed docs only after verification, and
> commit with `API-01: Introduce versioned application routes`. Do not start
> `API-02`.

After the implementer reports, use the subagent-driven-development task review
and broad branch review gates before integrating. Do not mark `API-01` complete
or advance the next action to `API-02` until those gates are clean.

## Deferred review items

These four Minor findings are intentionally outside `API-01` and remain open:

1. `resetAndSeedDevelopmentData()` truncates and seeds without one surrounding
   transaction, so a seed failure can leave disposable development fixtures
   empty or partial.
2. A rejected `pool.end()` during reset CLI cleanup is not handled separately
   from the preceding operation.
3. The mobile API base URL accepts query and hash components.
4. `server/src/index.ts` has no explicit `SIGINT`/`SIGTERM` shutdown path for
   the shared PostgreSQL pool.

Do not silently fold these into the API versioning task. Route each to its
proper later task or a separately approved maintenance change.

## Resume guardrails

- Use subagents for every implementation and review task.
- Do not build or test Expo web.
- Preserve the dedicated feature branch/worktree workflow.
- Keep secrets, token values, signed URLs, and seed passwords out of logs and
  documentation.
- Do not push, open a pull request, merge, or delete worktrees without the
  appropriate review/integration decision.
- Leave the Goal active unless the user changes its state in the Codex UI.
